
// Don't change the export values.
// You can use the function in the Math module

const sqrt = (number) => Math.sqrt(number);

const power = (base, exponent) => base ** exponent;

const round = (realNumber) => Math.round(realNumber);

export {
  sqrt,
  power,
  round,
};
